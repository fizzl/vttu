// JSX typings for the <altcha-widget> custom element. The runtime element is
// registered by importing the `altcha` package (see ApplicationForm.tsx); this
// pulls in the package's React JSX declarations so the element type-checks.
import "altcha/types/react";
