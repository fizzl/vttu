{
  "name": "vttu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build:web && npm run build:cdk",
    "build:web": "tsc -p tsconfig.web.json",
    "build:cdk": "tsc -p tsconfig.json",
    "test": "npm run test:lambda",
    "test:lambda": "cd lambda && go test ./...",
    "synth": "npm run build && cdk synth"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "aws-cdk": "^2.1029.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.205.0",
    "constructs": "^10.4.2"
  }
}
